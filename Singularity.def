Bootstrap: docker
From: centos:7
OSversion: 7
Include: yum wget
# If you want the updates (available at the bootstrap date) to be installed
# inside the container during the bootstrap instead of the General Availability
# point release (7.x) then uncomment the following line

# UpdateURL: http://yum-repos.hpccluster/centos/7/updates/$basearch/

%test
    python3.6 --version
    pip3.6 --version
    gcc --version
    make --version

    python3.6 -c "import numpy"


%environment
    export PATH="/usr/local/bin:$PATH:/usr/local/conda/bin/"
    source /usr/local/conda/bin/activate
    conda activate python36
    alias python=python3.6

%post
    mkdir /jic
    mkdir /nbi
    mkdir /tgac
    mkdir /ei
    mkdir /tsl
    mkdir /opt/software

    ### Install your packages ###
    cd /opt/software/

    yum -y install centos-release-scl
    yum -y install scl-utils
    yum -y install devtoolset-7-gcc* git which bzip2
    scl enable devtoolset-7 bash
    yum -y install gcc gcc-c++ make wget
    mkdir -p /etc/profile.d/
    echo -e '#!/bin/bash\nsource scl_source enable devtoolset-7' > /etc/profile.d/enablegcc7.sh
    gcc --version
    make --version

    # Clean up yum
    yum clean all

    cd /usr/local/src
    wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh
    bash Miniconda3-latest-Linux-x86_64.sh -b -p /usr/local/conda
    export PATH="/usr/local/conda/bin:$PATH"
    source /usr/local/conda/bin/activate
    conda update -n base -c defaults conda
    conda create -n python36 python==3.6
    conda activate python36
    conda install -y -c conda-forge pip

    python3 --version
    cd $(dirname $(which python3))
    cd /opt/software/

    # Install python requirements
    git clone https://github.com/EI-CoreBioinformatics/mikado.git
    cd mikado
    conda install --update-all -y -c conda-forge -c bioconda -c anaconda --file <(sed 's/;.*//' requirements.txt)
    python setup.py bdist_wheel
    pip3.6 install --prefix=/usr/local/ dist/*whl
    mikado --help

%apprun snakemake
	snakemake "@"

%apprun mikado
	mikado "@"

